import { css } from "@emotion/react";

export const translatorContainerStyles = () => css`
	&.translatorContainer {
		display: flex;
		justify-content: center;
		align-items: center;
		flex-direction: column;
		height: 100%;
	}
`;
